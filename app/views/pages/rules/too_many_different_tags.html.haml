:ruby
  title = 'Too Many Different Tags'
  description = <<~DESC
    If you have lots of different tags, it turns into tag-soup and becomes hard to read. Be wary of
    hiding behaviour behind tags.
  DESC

= render partial: 'pages/rules/layout', locals: { title: title,
                                                  description: description}